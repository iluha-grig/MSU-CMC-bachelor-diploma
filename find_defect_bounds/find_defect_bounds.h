#pragma once

#include <vector>


std::vector<float> find_defect_bounds(const std::vector<std::vector<float*>>& spectrums);


// от позиции +5 и -5 измерений (спектров) сразу по всем 64 датчикам
